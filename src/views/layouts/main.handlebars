<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Título de la Aplicación</title>
    <link rel="stylesheet" href="/styles.css"> <!-- Agrega tu archivo CSS -->
</head>
<body>
    <header>
        <nav>
            <ul>
                <li><a href="/">Inicio</a></li>
                <li><a href="/register">Registrarse</a></li>
                <li><a href="/login">Iniciar Sesión</a></li>
                {{#if user}} <!-- Verifica si el usuario está autenticado -->
                    <li><a href="/logout">Cerrar Sesión</a></li> <!-- Opción para cerrar sesión -->
                    <li>
                        {{#if user.cart}} <!-- Verifica si el usuario tiene un carrito -->
                            <a href="/cart/{{user.cart}}">Carrito</a> <!-- Usa el ID del carrito -->
                        {{else}}
                            <span>Carrito vacío</span> <!-- Mensaje si no hay carrito -->
                        {{/if}}
                    </li>
                {{/if}}
            </ul>
        </nav>
    </header>

    <main>
        {{{body}}} <!-- Aquí se renderizará el contenido de las otras vistas -->
        
        {{#if messages.success}} <!-- Mensajes de éxito -->
            <div class="alert alert-success">
                {{messages.success}}
            </div>
        {{/if}}
        {{#if messages.error}} <!-- Mensajes de error -->
            <div class="alert alert-danger">
                {{messages.error}}
            </div>
        {{/if}}
    </main>

    <footer>
        <p>&copy; 2024 Tu Aplicación</p>
    </footer>
</body>
</html>
